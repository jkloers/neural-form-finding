# Tesseract Oracle — Python dependencies
#
# tesseract_core is injected automatically by the Tesseract build system.
# SOFA and SofaPython3 are installed via custom_build_steps in tesseract_config.yaml.

numpy
scipy
